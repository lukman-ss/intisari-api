<?php

declare(strict_types=1);

namespace Database\Seeders;

use PDO;

class DatabaseSeeder
{
    public function run(PDO $pdo): void
    {
        // Add your demo users or initial data here.
        // For example:
        // $pdo->exec("INSERT INTO users (name, email) VALUES ('Demo', 'demo@example.com')");
    }
}
